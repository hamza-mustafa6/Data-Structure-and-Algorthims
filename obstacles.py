import arcade
from random import random

class Obstacles:
    def __init__(self, x, y):
        self.obstacle_xpos = x
        self.obstacle_ypos = y
        # self.obstacle_xpos = random() * (width - 200) + 100
        # self.obstacle_ypos = random() * (height - 100) + 200


